extends ../layout

block content
    .row
        .col-md-12
            .order-feild
                h2 支付提交
                p 订单详情内容可通过查看
                    a(href='#{adminDir}/usercart/myorder') 我的订单
                    | 进行核对处理。

            .order-feild
                p 订单号：
                    span(style='font-size:16px') #{orderdata.id}
                p 支付金额：
                    span(style='color:red') #{orderdata.price}

            .order-feild
                h2 支付方式
                .pay-type
                    a.btn.btn-danger.btn-xs.del-cart(href='#', data-id='#{orderdata.id}') 微信
                    a.btn.btn-danger.btn-xs.del-cart(href='#', data-id='#{orderdata.id}') 支付宝


